# Typecastinig = The process of converting a variable from one data type to another
#                str(), int(), float(), bool()

name = "Reece Tunesi"
age = 24
gpa = 3.2
is_student = False

print(type(name))           # Displays the type of variable.

gpa = int(gpa)              # Converting variables.
print(gpa)